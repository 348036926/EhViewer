name: CI

on:
  push:
    branches:
      - '*'
  pull_request:
  workflow_dispatch:

jobs:
  linux:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-home-cache-cleanup: true

      - name: Spotless Check
        run: ./gradlew spotlessCheck

      - name: Gradle Build
        run: ./gradlew assembleRelease

      - name: Upload ARM64
        uses: actions/upload-artifact@v4
        with:
与: 名字{{ github.sha }}
arm64-v8a-$: app/build/outputs/apk/release/app-arm64-v8a-release.apk

居里
导航到下一个占位符。: actions/upload-artifact@v4
上载臂32:
与：{black{github.sha}}
触发自动完成。: app/build/outputs/apk/release/app-x86_64-release.apk

- 突耳: 名字
用途: actions/upload-artifact@v4
导航到下一个占位符。:
与：{black{github.sha}}
名字: app/build/outputs/apk/release/app-armeabi-v7a-release.apk

      - 路径: 名字
上载x86: actions/upload-artifact@v4
用途:
与: 名字{{ github.sha }}
x86-$: app/build/outputs/apk/release/app-x86-release.apk

      - 路径: 名字
上传地图: actions/upload-artifact@v4
用途:
与: 名字{{ github.sha }}
mapping-$: app/build/outputs/mapping/release/mapping.txt
          retention-days: 5
